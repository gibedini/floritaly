### globals.R
### tells the system the names of global variables to avoid notes at check time

utils::globalVariables(c("cardinality", "ckl_names", "ckl_parsed", "ckl_version", "closest_match",
                         "distance", "myname", "quality", "verbatim", "verbatim.x", "verbatim.y"
                         ))
